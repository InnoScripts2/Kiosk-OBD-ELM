- `db/migrations` — PostgreSQL миграции. Переносим в инфраструктурный репозиторий, из Android проекта удаляем.
- `docs/`, `docs-unified/legacy` — большая документация, аналитика, R&D. По текущему требованию документацию исключаем (архив).
- `assets/` (icons, supabase JS) — фронтенд артефакты; либо удалить, либо перевести в Android-ресурсы (минимально необходимые иконки).
- `certs/`, `keys/` — dev сертификаты и ключи. Не храним в Android-репозитории; перенести в секретное хранилище или удалить.
- `logs/` — исторические логи. Переместить в архив вне проекта.
Session 1 Notes — Android Refactor

Focus: identify existing Android modules, mark for migration into unified minimal architecture.

Keep & migrate into new structure:
- apps/android-kiosk/app/src/main/AndroidManifest.xml → future app module
- apps/android-kiosk/app/src/main/kotlin/com/selfservice/kiosk/ble/recovery/BleReconnectCoordinator.kt → feature-obd recovery
- apps/android-kiosk/app/src/main/kotlin/com/selfservice/kiosk/ble/session/BleSessionStateMachine.kt → feature-obd session orchestration
- apps/android-kiosk/app/src/main/res/** → сейчас только `menu/` и `xml/`; отсутствуют `values/` и `mipmap`, требуется восстановить минимум (`ic_launcher`, `app_name`, темы) перед переносом
- apps/android-kiosk/ (корень) → добавлены вспомогательные `build.gradle`, `settings.gradle`, `gradle.properties` для попытки сборки; без восстановления ресурсов проект не выпускает APK, baseline фиксируем по unified-сборке
- Окружение сборки: JDK 17, Android SDK 34.0.0, Gradle 8.7 (wrapper из `apps-unified/android-kiosk`), AGP 8.4.1, Kotlin 1.9.24
- modules/device/obd/** → diagnostic core to refactor into feature-obd module
- modules/device/thickness/** → thickness measurements; migrate to feature-thickness module
- modules/service/payments/** → migrate relevant Kotlin/Java code into feature-payments (remove node_modules later)
- modules/service/reports/** → reporting logic for local generation, to be assessed for inclusion or archival

Archive/remove candidates (non-Android stacks):
- apps/kiosk/** (web frontend)
- apps/kiosk-agent/** (Node.js agent)
- apps/admin/** (web admin panel)
- node-legacy/**
- functions/**, supabase/**, infra/**, infra-unified/**
- docs/**, plan-*.md, README.md (non-Android) — move to archive per instruction to exclude documentation
- modules/service/**/node_modules/** — remove once Kotlin replacement ready

Pending inspection next session:
- apps/android-kiosk/app/src/main/kotlin/com/selfservice/kiosk/** beyond ble/
- modules/service/payments/core/src/** actual Kotlin sources
- modules/device/obd/andr/** vs diagnostics/** content
- New library folder from user (awaiting addition)
- Shared/ packages/ libs/ directories for reusable code

Next steps: deeper file-by-file classification in session 2.

---

Session 2 Notes — Deep dive per directory (in progress)

- Создан корневой Gradle-каркас `android/` (settings.gradle.kts, build.gradle.kts, gradle/libs.versions.toml) — база для переноса модулей.
- Перенесён `android/app`: Kotlin DSL `build.gradle.kts` с viewBinding, baseline зависимостями, `MainActivity` (AppCompat + WebView + жест настройки URL), ресурсы `values/`, `drawable/`, `layout/activity_main.xml`, `xml/network_security_config.xml`, `proguard-rules.pro`.
- Следующие действия: подготовить `android/core` (общие настройки, utils), наметить структуру `feature-obd-*`, синхронизировать Gradle wrapper.

---

Session 3 Notes — Core scaffolding (in progress)

- Создан модуль `android/core` (android-library + kotlin-android, minSdk 26, compileSdk 34) с базовыми зависимостями (`kotlinx-coroutines-core`, `androidx.core-ktx`).
- Добавлен `DispatchersProvider` для централизованного управления корутинными диспетчерами (IO/Default/Main).
- `android/app` подключён к `:core` (`implementation(project(":core"))`).
- Требуется собрать список общих утилит (permissions, logging, configuration) и подготовить тестовую инфраструктуру (`unitTest`, `androidTest`).
- Создан модуль `android/feature-obd-core` (library + kotlin-android, зависимость на `:core` + coroutines).
- Добавлены базовые модели `BleDevice`, `BleScannerConfig`, `ObdSessionState` как целевая оболочка для переноса `BleReconnectCoordinator`, `BleSessionStateMachine`, DiagnosticsSessionController.
- Определены интерфейсы `TransportFactory`, `ObdTransport`, `ObdSessionController`, `ObdSessionConfig`, базовые DTO `TransportFrame`/`TransportConnectionResult` для унификации BLE/PassThru каналов.
- Настроены тестовые зависимости (`junit`, `kotlinx-coroutines-test`) для `core` и `feature-obd-core`, добавлен `TestDispatchersProvider`, smoke-тест `ObdSessionStateTest`.
- Следующие шаги: реализовать mock-транспорт/сканер для unit-тестов state machine, начать перенос логики `BleReconnectCoordinator`, `BleSessionStateMachine`, DiagnosticsSessionController` в новые интерфейсы.

apps/android-kiosk/app/src/main/kotlin/com/selfservice/kiosk/
- Current structure только `ble/` (recovery, session). Проверить, нет ли остальных пакетов в других ветках; если отсутствуют, нужно создать базовые пакеты под ui/core при переносе.

modules/device/obd/andr/
- TypeScript пакет (`package.json`, `tsconfig.json`, src/commands.ts, pids/, dtc/). Предназначен для генерации/шаринга данных; потребуется конверсия в Kotlin или перенос логики в общую локальную БД. Классифицировать как источник данных → мигрировать в Kotlin-модуль (feature-obd-data).
- index.ts содержит каркас ElmSession/ElmCommand → переписать на Kotlin; файлы `pids/*`, `dtc/*` — источники данных для локальной БД.
- data/pids.json, conversions.json, dtc.json — первоисточники справочников; scripts/import-pids.mjs, validate-*.ts автоматизируют загрузку/валидацию. При переносе конвертировать JSON в Kotlin ресурсы или Room.
- pids/catalog.ts и dtc/catalog.ts реализуют поиск/нормализацию через require/createRequire. При переносе заменить на Kotlin репозитории, формулы из conversions.ts переписать.
- scripts/plan.md фиксирует процесс очистки PID-таблиц от GPL-источников — важно сохранить как документацию, но в итоговый Android репо лучше перенести в отдельный compliance-док.

modules/device/obd/diagnostics/obd-diagnostics/
- Ещё один TS-пакет (src/, dist/). Нужно извлечь алгоритмы диагностики, переписать на Kotlin. Файлы отметить как перенос в `feature-obd-core`.
- src/ содержит полный стек (drivers, connection manager, session manager, cache, retry, profiles, tests). Все ключевые классы (ObdConnectionManager.ts, Elm327Driver.ts, KingbolenEdiagDriver.ts, ObdSelfCheck.ts, metrics.ts и т.д.) → перенос в Kotlin (инфраструктура и тесты в Kotlin).
- KingbolenEdiagDriver.ts и KingbolenEdiagDriver_NEW.ts — две итерации BLE драйвера; при миграции выбрать актуальную и сверить метрики/идентификацию адаптера.
- DiagnosticSessionManager.ts (~800 строк) — центральный оркестратор с таймлайном событий, подсчётом MTBF/MTTR, хранилищем DiagnosticsEventStore. Нужно портировать аналитику и хранение (возможно, Room + Flow).
- driver/DeviceObd.ts описывает единый интерфейс и события адаптера, рядом acceptance/implementation отчёты и прометей-интеграция. При перезаписи в Kotlin зафиксировать контракт (status enum, события) и готовить KMP-friendly API.
- components/ (connection-policy, decoder-registry, metrics, state-model) — модульные сервисы для менеджера. Переносим в Kotlin без изменения публичных типов.
- events/ObdEvents.ts, types.ts — центральный EventEmitter API. При переносе заменить на Kotlin Flow/StateFlow, сохранив события.
- database/ (DtcDatabase.ts, PidDatabase.ts, dtc-codes.json) — источники справочных данных; преобразовать в Room/SQLDelight или Kotlin JSON. JSON сохранить и конвертировать в Android ресурсы.
- autoDetect.ts, bluetoothAutoDetect.ts, transports.ts — автоматическое определение транспорта (BLE). Интегрировать в Android сервис автоподключения, убрать Node EventEmitter.
- commands/Elm327Commands.ts и parsers (DtcParser.ts, Elm327Parser.ts, PidParser.ts) — прямой перенос в Kotlin, формулы/битовые маски оставить неизменными.
- polling/PollingManager.ts — очередь опроса и rate limiting; переписать на coroutines/Flow, предусмотреть WorkManager для фонового режима.
- driver/transport/ и драйверные прометей-метрики не переносить напрямую, но учесть метрики при проектировании Android telemetry.
- driver/transport/DevTransport.ts — DEV мок для Node окружения; в Android заменить на локальный симулятор/инструментальные тесты.
- tests/ и *.test.ts описывают сценарии подключения, кеша, ретраев — использовать как тест-кейсы для Kotlin unit/instrumented тестов.
- tests/ (BluetoothObdDriver.test.ts, DtcParser.test.ts, Elm327Parser.test.ts, PidDatabase.test.ts, PollingManager.test.ts) — прямое руководство для будущего покрытия.
- driver/examples/ содержит сценарии (подключение, polling, очистка DTC, Prometheus). Перенести как kotlin sample/integration tests.
- driver/__tests__/ (Elm327Driver, integration, parseDtc, parsePid, stress) — ориентиры для Kotlin тестов и нагрузочных сценариев.

modules/service/payments/core/
- Node/TypeScript реализация (package.json, src/, dist/, node_modules/). Решение: выделить интерфейсы, переписать на Kotlin или интегрировать только нужные алгоритмы; остальное удалить.
- src/ содержит пары `.ts/.js` (index, payment-module, prometheus) → переписать на Kotlin; после переноса удалить JS.
- payment-module.ts реализует эмуляцию платежей и экспорт Prometheus метрик; при переносе сохранить контракты createIntent/getStatus и заменить метрики на Android-совместимые (например, Firebase/встроенные счётчики).
- PaymentModule хранит intents в памяти, поддерживает dev/manual confirm и metrics snapshot. В Kotlin можно реализовать как репозиторий поверх Room.
- prometheus.ts — заглушка collector, регистрирующая метрики через переданный registry. В Android заменить на внутр. telemetry или вырезать.

modules/service/reports/** (ожидается аналогичная структура) — проверить в следующем шаге.
- modules/service/reports/reporting/src — генерация отчётов (diagnostics/thickness) + email/sms сервисы на Node. При миграции переносим бизнес-правила в Kotlin сервис, а текущие JS/TS удаляем после переноса. dist/ и node_modules/ — артефакты, не включать в новый репозиторий.
- diagnostics-report.ts собирает PDF через `pdfkit`, sms/email сервисы используют nodemailer/REST-интеграции. При переносе подобрать Android PDF/Share API и Jetpack WorkManager для фоновой отправки.
- sms-service.ts — в DEV режиме логирует, реальных провайдеров нет (TODO). В Android заменить на WorkManager + выбранный SMS/Push канал или оставить уведомления в UI.
- types.ts определяет структуры отчётов и конфиг поставщиков; можно использовать как основу для Kotlin data classes.

Следующий проход:
- Просмотреть `modules/service/reports/` и `modules/device/thickness/`.
- Оценить каталоги `shared/`, `libs/`, `packages/`.
- Дождаться новой папки с библиотеками пользователя и классифицировать.

Дополнительные наблюдения:
- modules/device/thickness/ — пусто; создать или удалить при рефакторе.
- modules/service/reports/reporting — Node/TS; алгоритмы отчётов переносим в Kotlin, остальное исключаем.
- src/ содержит `.ts/.js` для генерации отчётов (diagnostics/thickness, email/sms). Переносим в Kotlin, вычищаем JS/Node.
- shared/config, shared/utils — полностью пустые, удаляем при чистке.
- libs/cpp/ — заготовка CMake (пустой каталог cmake/, `include/selfservice/obd/Core.hpp`, `src/Core.cpp`, tests/README). Сейчас только функция `stub_diagnostic()` → 0; решить, либо удалить, либо использовать как основу для будущего JNI.
- libs/python/ — минимальный пакет `selfservice_diagnostics` (baseline.py + тест `test_baseline.py`). Логика ограничена подсчётом DTC, лучше переписать на Kotlin и архивировать Python.
- packages/andr-obd-import, green-api, pass-thru-jni — выяснить, какие используются. Вероятно, `pass-thru-jni` важно для JNI-доступа к пасстру адаптерам → перенести в Kotlin-проект (например, модуль `feature-obd-jni`). Остальные в архив, если не подтвердится необходимость.

Прочее:
- src/** — Vite/React приложение (App.tsx и т.д.). Подлежит перемещению в архив или удалению.
- tmp-emulator-test/ — JS-эмулятор ELM327 (ELM327Emulator.js, тесты, выгрузка DTC в migration-output/). Нужен только как参考 для Kotlin интеграционных тестов; папку вынести в архив.
- tools/data-migration (sign_package.py, prepare_resources.py и т.д.) и tools/security/export_security_backlog.py — утилиты для desktop/Node пайплайна. При чистке вынести в ops-репозиторий или удалить.
- functions/payments-webhook.ts + подпапка — Deno Edge Function для Supabase. Решить: перенос логики в Kotlin backend или открепление от Android проекта.
- templates/dtc-manifest.json — шаблон манифеста DTC-пакета (packageId, version, checksum). Перенести в doc/compliance или удалить, если данные будут генерироваться приложением.

Upcoming deep dive:
- Расписать содержимое `packages/**` и `libs/**` по модулям.
- Проверить `app/src/main/res/**` (menu/, xml/) — что переносим, что оптимизируем.
- Проанализировать `app/src/test/` и `build/` (удалить/пересобрать).

Дополнительно:
- app/src/main/res/menu/, res/xml/ — пустые. Можно удалить либо заполнить по мере разработки.
- app/src/test/... содержит тесты для BLE (BleReconnectCoordinatorTest.kt, BleSessionStateMachineTest.kt). Переносим вместе с кодом в новый модуль тестов.
- app/build.gradle использует минимальный набор зависимостей, minSdk 24, compileSdk 34. При переносе создать новый root build + settings.gradle.
- apps/android-kiosk имеет собственный `.git/` и icon-gen скрипты. При объединении перенести скрипты в `android/tools/`, `.git/` удалить.

Дополнительно по `packages/`:
- pass-thru-jni — CMake + include/src. Требует интеграции в Android через JNI. Помечаем для переноса в модуль `feature-obd-jni`.
- packages/pass-thru-jni содержит CMakeLists, include/selfservice/pass_thru/PassThruBridge.hpp и реализацию PassThruBridge.cpp. Готовая основа для JNI моста; перенести в Android NDK модуль, оставить только исходники и удалить лишний packaging.
- green-api — Node/TS проект (отправка отчётов, интеграции). Кандидат на архив.
- andr-obd-import — содержит субпроекты `androbd/`, `library/`, `oran/`, `plugin/`. Требует анализа, какой код полезен; вероятно, часть Android-кода можно мигрировать в `feature-obd-tools`, остальное в архив.
- Поддиректории `oran/` и `plugin/` пустые — удалить.
- `androbd/src/main/` — полноценный Android-проект (Manifest, java/com/..., res/). Требуется выборка полезных компонентов (OBD UI, сервисы) → перенос в новый `feature-obd-ui` или `feature-obd-core`.
- andr-obd-import/androbd/java содержит пакет `com.fr3ts0n.ecu.gui` (activities, widgets) — использовать как справочник UI/формул, но код Java → Kotlin миграция, лишнее архивируем.
- `library/src/main/` и `test/` — библиотека с пакетами `com.fr3ts0n.common|ecu|prot|pvs` (общие парсеры, UI виджеты). При переносе выделить полезные алгоритмы (формулы, enum) и переписать на Kotlin; остальное в архив.
- `library/src/main/java/com/fr3ts0n/ecu` содержит Conversion, EcuDataItems, ObdCodeList и протокольные пакеты (obd/vag). Важно для формул/расчётов PID — извлечь и перевести в Kotlin util.
- `pass-thru-jni/src/PassThruBridge.cpp` — основной C++ мост, переносим в NDK-модуль.

Дополнительные артефакты (из новых папок):
- `AndrOBD-master/` — полный Android-проект с собственными модулями (`androbd`, `library`). Использовать как источник кода для OBD UI/логики при переносе в новые feature-модули; остальное архивировать.
- `Bluetooth-OBD-II-Diagnostic-Tool-main/` — Android-приложение с активностями по режимам OBD (Mode 1–9). Использовать как источник UI/формул; адаптировать полезные части в `feature-obd-ui`/`feature-obd-modes`.
- Основные пакеты: Mod_1…Mod_9 (activities, response calculators), PIDsEnums (enum запросов). Нужны для справочника формул и UI pattern → перенести только алгоритмы/enum, переписать на Kotlin.
- `android-obd-reader-master/` — Java-библиотека и приложение (obd-api, obd-reader). Импортировать важные команды/сервисы в Kotlin (core diagnostics), остальное в архив.
- `obd-api` содержит иерархию команд (ObdCommand, enums, protocol, utils) + тесты; портируем в Kotlin (core/commands).
- `obd-reader` — старое приложение (activities, custom gauges, network). Использовать как inspiration для UI, но код legacy → архив или адаптация по необходимости.
- `functions/` (payments-webhook.ts + папка) — Deno Edge Function для Supabase; функционал платежного вебхука переписать на Kotlin или сохранить как внешний сервис. При чистке переносим логику в Kotlin-бэкенд либо удаляем.
- `packages/green-api/` — Node/TS скрипты для отправки WhatsApp-отчётов. Подлежит архиву; если функционал нужен, интегрировать в Kotlin (или отдельный облачный сервис), но текущее содержимое не соответствует минималистичному Android-стеку.
- src/ (client, report-sender, types + тесты) → при необходимости перенести в Kotlin модуль фоновой отправки, Node-версию удалить.
- client.ts инкапсулирует REST-запросы (instanceId/apiToken), report-sender.ts собирает диагностический отчёт. Контракты зафиксировать для возможного Android HTTP-клиента.
- `libarty/` (carloop-library) — Arduino/C++ библиотека (TinyGPS++, carloop.cpp/h). Использовать как справочник для CAN/телеметрии; прямой перенос невозможен, архивируем или извлекаем формулы.
- `libs/cpp` и `libs/python` — см. выше: сейчас чистые заглушки (stub_diagnostic, baseline heuristics). Либо удалить, либо перенести минимальные формулы в Kotlin.
- `supabase/migrations/` — SQL миграции для безопасности. Сохранить в отдельном репозитории инфраструктуры. В Android-монорепе можно оставить ссылку или удалить (по правилу «без документации/серверов»). Функционал Supabase предполагает внешний сервис.

Большие агрегаты:
- `apps-unified/` — содержит повторяющиеся проекты (android-kiosk, kiosk-agent, админ-панель, frontend). Требует полной разборки: Android-код извлечь в новые модули, остальное архивировать. Документация (`IMPLEMENTATION_*.md`) и чеклисты — в архив.
- `apps-unified/android-kiosk` — ещё один Gradle-проект с подмодулями (diagnostics-core, serialport). Использовать как источник Kotlin-кода (diagnostics-core → core/diagnostics), сохранив минимально необходимое. Удалить дублирующие build.gradle/settings после переноса.
- app/src/main/java/com/selfservice/kiosk/MainActivity.kt — WebView-обёртка киоска с жестом (3 пальца, 5 секунд) для смены URL. Для нативного приложения ненужна; если сохраняем, переносим в отдельный kiosk-shell модуль.
- res/layout/activity_main.xml — FrameLayout с WebView, служит текущей оболочкой.
- app/src/main/java/com/selfservice/kiosk/bluetooth/** и diagnostics/** — legacy активности/модели (EldiagDiagnosticsActivity и т.д.). При консолидации решить: переносить отдельные компоненты или удалить в пользу нового Kotlin UI.
- app/src/main/java/com/selfservice/kiosk/bluetooth/EldiagDiagnosticsActivity.kt — готовая BLE-ориентированная активность со сканером, проверкой разрешений и обработкой ElmCommandHandler. Использовать как эталон бизнес-потока (сканирование → подключение → чтение/сброс DTC), но при переносе переписать на современную навигацию/Compose и вынести бизнес-логику в ViewModel/сервисы из нового `feature-obd-session`.
- diagnostics/* в app модуле пустые — можно удалить либо наполнить ссылками на `diagnostics-core` модуль.
- bluetooth/connection/BleGattConnection.kt — полноценный BLE стек (MTU, DIS проверка серийника, Nordic UART). Реализует coroutine-based connect с экспоненциальным бэкоффом, чтением DIS serial для валидации и deferred-колбэками на discovery; оставить архитектуру, перенести в `feature-obd-transport` с выделением BLE сервиса и DI.
- bluetooth/connection/ClassicSppConnection.kt — RFCOMM SPP коннектор с IO-потоками и экспоненциальным бэкоффом. Перенести в отдельный транспорт, завернуть input/output в suspending API + фреймирование.
- bluetooth/connection/ConnectionFactory.kt — синхронный выбор BleGattConnection vs ClassicSppConnection. В новом проекте заменить на инъецируемый фабричный сервис с fallback/telemetry.
- bluetooth/data/* — Elm транспорт реализации (BleElmTransport, SppElmTransport, ElmCommandHandler). ElmCommandHandler уже инкапсулирует init-пайплайн AT-команд (ATZ→ATSP0), чтение PID/DTC, voltage/protocol; переносим как доменный сервис, чтобы UI/активности обращались к нему через репозиторий.
- bluetooth/scanner/* — набор сканеров BLE/Classic с unified façade; `UnifiedBluetoothScanner` комбинирует два Flow, нормализует по MAC и сортирует по target serial/RSSI. Сценарий оставить, но вынести в отдельный use-case + permissions layer.
- bluetooth/models/* — интерфейсы `AdapterConnection`, `ScanResultModel` (с проверкой серийника `TARGET_SERIAL_NUMBER = 979868808198`, эвристикой `isEldiagCandidate`). При переносе предусмотреть конфигурируемые serial/pattern вместо захардкоженных значений.
- bluetooth/permissions/BluetoothPermissionManager.kt — управление разрешениями для BLE/Classic с API level-aware списком (S+: SCAN/CONNECT). Перенести как utility, но перевести на ActivityResultContracts.
- diagnostics-core/src/main/java/com/selfservice/kiosk/diagnostics/{core,elm,passthru,session} — актуальные Kotlin/Java реализации диагностики, ошибки, Elm327 слои. Включить в новый `feature-obd-core` (перепроверить зависимости).
- diagnostics-core/session/DiagnosticsSessionController.kt — coroutine-базовый контроллер PassThru (open, establishChannel, clearDtc) со статами Idle/Connecting/Ready/Failed и хранением последнего adapterId. Переносим логику в ViewModel/use-case, оставляем PassThruClient контракт.
- diagnostics-core/core/** — `DiagnosticsErrorCode` и `DiagnosticsException` задают доменную модель ошибок; делаем их публичными константами нового SDK.
- diagnostics-core/elm/ElmResponseParser.kt — парсер DTC из ELM-ответов, использует bitmasking и фильтрацию токенов. Переносим без изменений, добавляем unit-тесты и покрытие edge-case (много 43).
- diagnostics-core/passthru/PassThruClient.kt — high-level клиент поверх `PassThruTransport` с Mutex и withTimeout, маппингом ошибок в DiagnosticsException. Мигрировать в core-service, оставить API (open/close/establishChannel/send/receive/clearDtc) и вынести transport под DI.
- diagnostics-core/passthru/PassThruContracts.kt — набор data-контрактов (AdapterOptions, Frame, ReceiveResult, ClearStrategy). Использовать как основной API слоя passthru.
- diagnostics-core/passthru/DevPassThruTransport.kt — dev-заглушка с задержками, вернуть как debug implementation; продакшен реализацию повесить на JNI мост.
- diagnostics-core/passthru/PassThruTransport.kt — базовый интерфейс для JNI/Dev реализаций; выносим в отдельный модуль, чтобы NDK-библиотека его реализовывала.
- serialport/src/main/java/world/shanya/serialport/** — сторонняя библиотека (SerialPortConfig, discovery/server/…) со множеством вспомогательных классов. Проверить лицензии, при переносе разместить как отдельный module или заменить ссылкой на OSS зависимость.
- serialport/ — Android библиотека с манифестом, ресурсами и иконками устройств; пригодится как модуль `feature-obd-serialport` или заменить Jetpack-плагином. Проверить лицензии перед переносом.
- `packages-unified/obd-andr` — TypeScript-пакет (dtc.ts, pids.ts, elm327.ts, parser.ts). Полный дубль справочников из `modules/device/obd`; при чистке консолидировать в один источник данных (Kotlin), пакет удалить.
- `docs-unified/`, `archives/`, `todo/` и другие документные каталоги — переместить в архив вне Android-проекта.
- `public/` — web-assets (favicon, sw.js). Удалить.
- `tmp-emulator-test/` — скрипты для эмуляции ELM327 (JS). Перенести логику в Kotlin тесты, папку удалить.
- `tools/` (data-migration, security) — скрипты на Node/TS; перенести нужные алгоритмы или архивировать.
- `todo/` — пустой, удалить.
- `apps/kiosk/agent` — Node/TypeScript backend (миграции, scripts, src). Перенести только бизнес-логику в Kotlin (если нужна), остальное удалить.
- src/index.ts — Express сервер (~1500 строк) с OBD BLE/Serial логикой, платежами, Supabase, AI ассистентом, Prometheus. Использовать как справочник при переносе, но не включать в Android репо.
- `apps/kiosk-agent` — хранит `storage/devices.sqlite`; основной серверный код отсутствует (видимо перенесён). Файл базы → архив/удаление.
- `node-legacy/` — дубль legacy-структур (`apps`, `packages`). Полностью в архив.
- `infra/`, `infra-unified/` — docker, monitoring, systemd, scripts. В новом Android-репо исключаем; сохранить в отдельном ops-репозитории.
- `build/`, `dist/` — артефакты сборки (cpp-tests, dtc packages). Переместить в `archive/artifacts` или удалить.
- `archives/` — уже содержит legacy (android-kiosk-legacy, bluetoothspp). Можно оставить как архив, но вне основного Android дерева.

Implementation track — Session 1 (APK baseline)
- Целевой модуль для замеров: `apps/android-kiosk/app` (namespace `com.selfservice.kiosk`, minSdk 24, targetSdk 34, JDK 17).
- Альтернативный baseline: `apps-unified/android-kiosk/app` (Gradle wrapper в комплекте); сравнить размеры и состав зависимостей, чтобы определить, какой проект брать за исходную точку.
- Текущие зависимости Gradle: `androidx.core:core-ktx:1.13.1`, `androidx.appcompat:appcompat:1.7.0`, `com.google.android.material:material:1.12.0`, `kotlinx-coroutines-core/android:1.9.0` + тестовые `junit` и `kotlinx-coroutines-test`.
- Дополнительные зависимости `apps-unified/android-kiosk`: `lifecycle-runtime-ktx:2.8.4`, `kotlinx-coroutines-android:1.8.1`, модульные ссылки `:serialport`, `:diagnostics-core`, тестовые `mockito-core`, `mockito-kotlin`.
- Подпись release-сборки завязана на dev keystore (`kiosk-release.keystore`); для замеров использовать локальные значения или временно отключить подпись.
- План действий: 1) подготовить Gradle wrapper (или использовать имеющийся), 2) выполнить `./gradlew :app:assembleRelease`, 3) зафиксировать размер APK из `app/build/outputs/apk/release/*.apk`, 4) собрать список подключённых модулей/ресурсов, 5) сохранить данные в `android/session-logs/session-01.md` (создать при первом замере).
- Предварительные требования: проверить наличие JDK 17, Android SDK 34; при отсутствии — добавить в документацию зависимостей.
- Прогон 03.11.2025: `apps-unified/android-kiosk` → `app-release-unsigned.apk` 5 412 927 байт (см. `android/session-logs/session-01.md`).
